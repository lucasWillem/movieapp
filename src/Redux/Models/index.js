import alertsModel from "./Alert.model";
import favouriteMoviesModel from "./FavouriteMovies.model";

const model = {
  alerts: alertsModel,
  favouriteMovies: favouriteMoviesModel,
};

export default model;
